{{> label container=container name=name label=label}}
<input
  type="{{type}}"
  id="{{container}}__{{name}}"
  name="{{name}}"
  required
  placeholder="{{placeholder}}"
  class="{{container}}__field-{{type}}" />
